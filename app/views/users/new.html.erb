<% provide(:title, 'ユーザーアカウントの作成') %>
<h1>ようこそカテキョへ</h1>
<p class= text-center>メールアドレスを登録し、お気に入りの家庭教師を見つけましょう</p>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <div class="panel panel-default">
      <div class="panel-body form-padding-50">
        <%= form_with(model: @user, local: true) do |f| %>
          <%= render 'shared/error_messages' %>

          <%= f.label :name, class: "label-signup" %>
          <%= f.text_field :name, class: "form-control", placeholder: "お名前" %>

          <%= f.label :email, class: "label-signup" %>
          <%= f.email_field :email, class: "form-control", placeholder: "sample@sample.com" %>

          <%= f.label :password, class: "label-signup" %>
          <%= f.password_field :password, class: "form-control", placeholder: "半角英数字６文字以上" %>

          <%= f.label :password_confirmation, class: "label-signup" %>
          <%= f.password_field :password_confirmation, class: "form-control", placeholder: "確認パスワード" %>

          <%= f.hidden_field :parent, :value =>true %>

          <%= f.submit "メールアドレスで登録", class: "btn btn-primary btn-danger btn-lg btn-block btn-signup" %>
        <% end %>
      </div>
    </div>
  </div>
</div>